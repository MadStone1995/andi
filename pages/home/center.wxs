var substr = function (val) {

  if (val.length == 0 || val == undefined) {

    return false;

  } else if (val.length > 88) {

    return val.substring(0, 88) + "...";

  } else {

    return val;

  }

}
module.exports.substr = substr